NFT **白名单（Whitelist，简称 WL）** 是 NFT 项目方在正式开售前，提前给某些特定地址 **预留 Mint 权利** 的机制。

简单说：  
**白名单 = 优先购买资格 + 保证能 Mint 到 + 通常价格更便宜**

---

# 🧩 白名单是什么？（一句话版）

**白名单是“提前登记的允许 Mint 的地址清单”，只有在名单里的地址才能优先购买或以优惠价铸造 NFT。**

---

# 🧠 为什么需要白名单？

原因主要有三个：

### 1. **防止抢购 / Bot 狂 Mint**

公开发售容易被机器人抢完，真正支持项目的人抢不到。  
白名单让他们能优先购买，不被机器人干扰。

### 2. **奖励早期支持者**

比如关注 Twitter、加入 Discord、参与社区活动的人 ⇒ 优先资格。

### 3. **控制 Mint 流量，保护链上 gas**

白名单 Mint 通常分批执行，避免瞬间拥堵。

---

# 🛠 白名单在智能合约中怎么实现？

最常见的几种方式：

---

## **方式 1：Mapping 记录白名单地址**

```solidity
mapping(address => bool) public whitelist;

modifier onlyWhitelist() {
    require(whitelist[msg.sender], "Not in whitelist");
    _;
}
```

调用 mint 时：

```solidity
function whitelistMint(uint quantity) external onlyWhitelist {
    _mint(msg.sender, quantity);
}
```

---

## **方式 2：Merkle Tree（最流行）**

大型项目常用（如 BAYC、Azuki）

优点：

✔ 可证明地址存在  
✔ 不需要在链上存大量地址（节省 gas）  
✔ 不需要公开所有白名单内容（避免泄露）

Mint 函数里：

```solidity
function whitelistMint(uint256 quantity, bytes32[] calldata proof) external {
    require(
        MerkleProof.verify(proof, merkleRoot, keccak256(abi.encodePacked(msg.sender))),
        "Not in whitelist"
    );

    _mint(msg.sender, quantity);
}
```

---

## **方式 3：签名（EIP-712 / ECDSA）– 动态白名单**

项目方签署一条消息：

→ 地址 A 可以 mint 2 个  
→ 地址 B 可以 mint 1 个

Mint 时验证签名即可。

优点：  
✔ 可动态添加白名单  
✔ 不需要链上存储  
✔ 节省 gas

---

# 🤑 白名单能带来什么好处？

对用户：

- 以更低价格 Mint（Pre-sale price）
    
- 保证你一定 mint 得到
    
- 不需跟机器人拼 gas
    

对项目方：

- 保证初期忠诚用户
    
- 避免链上拥堵
    
- 控制供应和流量
    

---

# 📌 简单总结（适合复制进笔记）

```
NFT 白名单（Whitelist）是预先批准的地址列表，
拥有优先或独家铸造（Mint）NFT 的权利，一般比公开 Mint 便宜。

作用：
1. 防止机器人抢购
2. 奖励早期支持者
3. 控制 mint 流量与 gas

实现方式：
1. Mapping 记录白名单
2. Merkle Tree（最常用）
3. 签名验证（EIP-712/ECDSA）

白名单 Mint 通常：
- 在公开 Mint 之前进行
- 限购
- 降价
```
